//This enum class creates a method which show what Screen we are transition to
public enum ScreenController {
    HomeScreen,
    AuthenticationScreen,
    AccountScreen,
    BalanceScreen,
    DepositScreen,
    WithdrawScreen
}
